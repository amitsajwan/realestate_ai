def connect_whatsapp(agent):
    import logging
    logger = logging.getLogger(__name__)
    logger.info(f"Connecting WhatsApp for agent: {agent}")
    # Placeholder for WhatsApp cloud API integration
    # No paid messaging triggered unless enabled
    return True
