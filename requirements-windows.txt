# Windows-compatible requirements for PropertyAI Premium Mobile CRM
# This avoids compilation issues by using pre-built wheels

# Core FastAPI (no compilation needed)
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Configuration (pure Python)
pydantic==2.5.3
pydantic-settings==2.1.0
python-dotenv==1.0.0

# Templates (pure Python)
jinja2==3.1.2

# HTTP clients (pure Python)
requests==2.31.0
httpx==0.25.2

# AI Integration (pure Python)
groq==0.4.1

# Windows compatibility
colorama==0.4.6

# Authentication (pure Python)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Skip problematic packages for Windows testing
# numpy==1.26.4  # Skip - causes compilation issues
# motor==3.3.2   # Skip - requires numpy
# pymongo==4.6.0 # Skip - can cause issues

# Email validation (pure Python)
email-validator==2.1.0

# Additional utilities
typing-extensions==4.8.0